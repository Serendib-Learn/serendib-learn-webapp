{
  "name": "serendib-learn-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "API service for Serendib Learn. Local JSON document store now, MongoDB later.",
  "scripts": {
    "dev": "node --watch src/index.ts",
    "start": "node src/index.ts",
    "typecheck": "tsc --noEmit",
    "seed": "node src/scripts/reseed.ts"
  },
  "engines": {
    "node": ">=22.6"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "google-auth-library": "^11.0.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.3",
    "@types/node": "^24.9.2",
    "typescript": "^5.9.3"
  }
}
